/**
 * functions.js
 *
 * Created by jrootham on 13/10/14.
 *
 * Copyright © 2014 Jim Rootham
 */
(function () {
    "use strict"
    /*
    it('should return [] if any spec in Array matches argument', function(){
        var errorArray = argumentSpec.validate('option', [0, true], 0);
        assert.equal(0, errorArray.length);
    })

    it('should return [] if any spec in Array matches argument', function(){
        var errorArray = argumentSpec.validate('option', [0, true], false);
        assert.equal(0, errorArray.length);
    })

    it('should return [error] if no spec in Array matches argument', function(){
        var errorArray = argumentSpec.validate('option', [0, true], '');
        assert.equal(1, errorArray.length);
        assert.equal('option does not match any spec', errorArray[0]);
    })
*/
})()